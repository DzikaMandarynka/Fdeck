## Fdeck
a small CLI flashcard app built with Rust with no dependencies

# Commands
`fdeck help` -> displays help screen
`fdeck add_group <group_name>` -> adds a group
`fdeck add_card <card_name> <card_question> <card_answer>` -> adds a card
`fdeck review <group>` -> reviews a group
